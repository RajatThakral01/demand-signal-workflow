[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
markers =
    live: makes a real (paid) call to an external LLM provider; requires RUN_LIVE_INTERPRET_TEST=1 and a real OPENROUTER_API_KEY.